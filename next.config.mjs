/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/silcon_landing',
  output: 'export',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
};

export default nextConfig;
